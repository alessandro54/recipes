~replace~>navigator
  template
    = render 'components/calendars/navigator', date:
~replace~>calendar
  template
    = render 'components/calendars/calendar', calendar_days:, override_day: date != Date.today
