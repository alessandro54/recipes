ruby:
  weekdays = %w[Mon Tue Wed Thu Fri Sat Sun]
.lg:flex.lg:h-full.lg:flex-col.px-8.py-6
  - if params[:controller] == 'calendars'
    h1.text-4xl = calendar.title
    span.pl-2.text-xs.text-gray-400
      | by #{calendar.owners.pluck(:first_name).join(', ')}
  - else
    h1.text-4xl My calendar
  .h-max.lg:flex.lg:flex-auto.lg:flex-col.justify-center
    = render 'components/calendars/navigator', date: Date.today
    .grid.grid-cols-7.gap-px.border-b.border-gray-300.bg-gray-200.text-center.text-xs.font-semibold.leading-6.text-gray-700.lg:flex-none
        - weekdays.each do |day|
          .flex.justify-center.bg-slate-500.py-2.text-white
            span = day[0]
            span.sr-only.sm:not-sr-only = day[1..]
    .bg-slate-100.text-xs.leading-6.text-gray-700.lg:flex.shadow.ring-1.ring-black.ring-opacity-5
      = render 'components/calendars/calendar', calendar_days:, override_day: false

= render 'components/shared/modals/default', id: 'newDayModal', frame_id: 'new_day', title: 'Assign recipe'
